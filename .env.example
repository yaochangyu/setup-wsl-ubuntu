# WSL Ubuntu 安裝設定範例
# 複製為 .env 並填入實際值：
#   cp .env.example .env
#
# 注意：.env 含有機敏資料，已加入 .gitignore，請勿納入版控

# WSL 發行版完整名稱（例如: Ubuntu-22.04、Ubuntu-24.04、Debian）
# 若設定此項，UBUNTU_VERSION 會被忽略
# DISTRO_NAME=Ubuntu-22.04

# Ubuntu 版本（向下相容，若 DISTRO_NAME 未設定則自動轉換為 Ubuntu-XX.XX）
UBUNTU_VERSION=24.04

# WSL 使用者帳號（機敏）
WSL_USERNAME=your_username
WSL_PASSWORD=your_password
