# WSL Ubuntu 安裝設定範例
# 複製為 .env 並填入實際值：
#   cp .env.example .env
#
# 注意：.env 含有機敏資料，已加入 .gitignore，請勿納入版控

# ── WSL 發行版設定 ─────────────────────────────────────────
UBUNTU_VERSION=24.04

# ── WSL 使用者帳號（機敏）─────────────────────────────────
WSL_USERNAME=your_username
WSL_PASSWORD=your_password

# ── 系統設定 ──────────────────────────────────────────────
UPGRADE_PACKAGES=false
TIMEZONE=Asia/Taipei
LOCALE=en_US.UTF-8
ADDITIONAL_LOCALES=zh_TW.UTF-8

# ── Git 設定（選填）───────────────────────────────────────
GIT_USER_NAME=
GIT_USER_EMAIL=

# ── 代理設定（選填）───────────────────────────────────────
PROXY_URL=
NO_PROXY=

# ── Docker 設定 ────────────────────────────────────────────
DOCKER_WINDOWS_HOST=true
# DOCKER_TCP_PORT=2375  # 不設定則自動偵測可用 port（2375–2399）
# 鏡像加速站，多個以逗號分隔，例如
# DOCKER_REGISTRY_MIRRORS=https://mirror.example.com,https://mirror2.example.com
DOCKER_REGISTRY_MIRRORS=
OFFLINE_MODE=false
# 離線套件目錄路徑（OFFLINE_MODE=true 時使用），例如：
# OFFLINE_DIR=/mnt/d/offline-packages
OFFLINE_DIR=

# ── 語言版本設定 ───────────────────────────────────────────
NVM_VERSION=v0.39.7
PYTHON_VERSION=3.12
GO_FALLBACK_VERSION=1.24.1
# .NET SDK 版本，空白分隔，例如只裝 LTS：
# DOTNET_VERSIONS=6.0 8.0 10.0
DOTNET_VERSIONS=5.0 6.0 7.0 8.0 9.0 10.0
DOTNET_INSTALL_DIR=/usr/share/dotnet

# ── 資料庫設定 ─────────────────────────────────────────────
ACCEPT_EULA=Y

# ── 除錯 ──────────────────────────────────────────────────
DEBUG=false
